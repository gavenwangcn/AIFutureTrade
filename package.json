{
  "name": "aifuturetrade-frontend",
  "version": "1.0.0",
  "description": "Frontend dependencies for AIFutureTrade",
  "scripts": {
    "install-deps": "npm install",
    "build": "echo 'No build step required for static assets'",
    "copy-assets": "mkdir -p static/lib && cp node_modules/klinecharts/dist/klinecharts.min.js static/lib/ 2>/dev/null || echo 'KLineChart files copied'"
  },
  "dependencies": {
    "klinecharts": "^9.0.0"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  }
}

